all:
  vars:
    ansible_user: ubuntu

  children:
    kubernetes:
      vars:
        ansible_become: true
      children:
        control_plane:
          hosts:
            k8s-cpl-01:
              ansible_host: k8s-cpl-01.home

        workers:
          hosts:
            k8s-wrk-01:
              ansible_host: k8s-wrk-01.home
            k8s-wrk-02:
              ansible_host: k8s-wrk-02.home